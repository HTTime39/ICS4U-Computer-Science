//Ask a user to input dimensions for the base and height of a triangle. Afterwards, create a program that calculates the area of the given triangle 

/*
PROMPT "What is the base of the triangle?"
PROMPT "What is the height of the triangle?"

PRINT base multiplied by the height divided by 2
*/

base = parseInt(prompt("What is the base of the triangle?"));
height = parseInt(prompt("What is the height of the triangle?"));

alert((base * height ) / 2);